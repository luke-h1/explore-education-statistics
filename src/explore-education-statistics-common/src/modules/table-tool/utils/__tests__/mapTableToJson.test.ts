import { mapTableToJson } from '../mapTableToJson';

describe('mapTableToJson', () => {
  test('should return a table with the correct number of rows', () => {
    // expect(mapTableToJson());
  });
});
